{% extends 'main.html.twig' %}
{% block sub_body %}
<h1 class="text-center">Oeuvres en attente de validation</h1>
<div class="text-center">
    <a href="{{ path('app_artwork_index') }}" class="btn btn-primary mb-4 mt-4">Retour à la liste des oeuvres</a>
</div>
<table class="table table-striped">
    <thead>
        <tr class="text-center">
            <th>Id</th>
            <th>Titre</th>
            <th>Description</th>
            <th>Image</th>
            <th>Valider</th>
        </tr>
    </thead>
    <tbody>
    {% for artwork in artworks %}
        <tr class="text-center">
            <td>{{ artwork.id }}</td>
            <td>{{ artwork.title }}</td>
            <td>{{ artwork.description }}</td>
            <td><img src="{{ artwork.picture }}" height="300px"></td>
            <td>
            <form action="{{ path('app_artwork_validate', {'id': artwork.id}) }}" method="post">
                <button class="btn btn-success">Valider l'oeuvre</button>
            </form>
            </td>
        </tr>
    {% else %}
        <tr class="text-center">
            <td colspan="7">Aucune oeuvre à valider pour le moment</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}