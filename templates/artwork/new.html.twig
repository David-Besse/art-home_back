{% extends 'main.html.twig' %}

{% block title %}Ajouter une oeuvre{% endblock %}

{% block sub_body %}
    <h1 class="text-center">Ajouter une oeuvre</h1>

    {{ include('artwork/_form.html.twig') }}
<div class="text-center">

    <a href="{{ path('app_artwork_index') }}" class="btn btn-outline-primary">Retour à la liste des oeuvres</a>
</div>
{% endblock %}
