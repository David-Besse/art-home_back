{% extends 'main.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block sub_body %}


<h1 class="text-center">Modifier un utilisateur</h1>
<div class="form">
    <span class="text-danger d-flex justify-content-start mb-5">* champs obligatoires</span>
    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
</div>

<div class="d-flex justify-content-around mb-3">

    <a href="{{ path('app_user_index') }}" class="btn btn-outline-primary">Retour à la liste des utilisateurs</a>

    {{ include('user/_delete_form.html.twig') }}
</div>

{% endblock %}